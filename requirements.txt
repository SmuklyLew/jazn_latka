# Compatibility/cache marker for tooling that watches requirements.txt.
#
# Canonical direct runtime dependencies live in:
#   pyproject.toml -> [project].dependencies
# Optional capability dependencies live in:
#   pyproject.toml -> [project.optional-dependencies]
#
# Reproducible offline installation uses Dependency Studio generated
# JAZN_WHEELHOUSE_REQUIREMENTS.txt files with exact pins and SHA-256 hashes.
# Do not duplicate dependency specifications or hand-author hashes here.
