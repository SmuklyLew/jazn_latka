# Pack Generator 10.1.86.0 snapshot

This directory preserves the exact tracked generator source set retired by the
`10.1.86.0.111` clean rewrite. The archived launcher, builder and source modules
are historical reference material only and are not imported by the active
Pack Generator.

The active implementation lives in `tools/jazn_pack_generator_app/` and the
public entrypoint remains `tools/jazn_pack_generator.py`.
