name: Obsolete v15.1.0.3.89 transport inspection

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  obsolete:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete transport. Do not run or merge PR #69."
