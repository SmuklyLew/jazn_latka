latka_jazn.tools.release_metadata_sync
-          --root .
-          --base-branch master
-          --write
-          --json
-
-      - name: Remove temporary repair workflow and commit
-        shell: bash
-        run: |
-          rm -f .github/workflows/v1625-type-audit-repair.yml .github/.v1625-type-audit-repair-trigger
-          git diff --check
-          git config user.name 'github-actions[bot]'
-          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
-          git add -A
-          git commit -m 'fix: repair v16.2.5 static type audit'
-          git push origin HEAD:backup/v16.2.4-pre-full-apply-20260824
--- a/sitecustomize.py
+++ /dev/null
@@ -1,117 +0,0 @@
-from __future__ import annotations
-
-import os
-import subprocess
-import sys
-from pathlib import Path
-
-_BRANCH = "backup/v16.2.4-pre-full-apply-20260824"
-_GUARD = "JAZN_V1625_REPAIR_ACTIVE"
-
-
-def _run(command: list[str], *, env: dict[str, str] | None = None) -> None:
-    subprocess.run(command, check=True, env=env)
-
-
-def _repair() -> None:
-    if os.environ.get("GITHUB_ACTIONS") != "true":
-        return
-    if os.environ.get(_GUARD) == "1":
-        return
-    if os.environ.get("GITHUB_HEAD_REF") != _BRANCH:
-        return
-
-    root = Path.cwd().resolve()
-    recovery = root / "latka_jazn/bootstrap/chatgpt_recovery.py"
-    version = root / "latka_jazn/version.py"
-    if not recovery.is_file() or not version.is_file():
-        return
-
-    text = recovery.read_text(encoding="utf-8")
-    replacements = [
-        ("from typing import Any\n", "from typing import Any, cast\n"),
-        (
-            "    sidecar = discovery.get(\"sidecar\") if isinstance(discovery.get(\"sidecar\"), dict) else {}\n",
-            "    raw_sidecar = discovery.get(\"sidecar\")\n"
-            "    sidecar = cast(dict[str, Any], raw_sidecar) if isinstance(raw_sidecar, dict) else {}\n",
-        ),
-        (
-            "        existing_sidecar = (\n"
-            "            existing_repack.get(\"sidecar\")\n"
-            "            if isinstance(existing_repack.get(\"sidecar\"), dict)\n"
-            "            else {}\n"
-            "        )\n",
-            "        raw_existing_sidecar = existing_repack.get(\"sidecar\")\n"
-            "        existing_sidecar = (\n"
-            "            cast(dict[str, Any], raw_existing_sidecar)\n"
-            "            if isinstance(raw_existing_sidecar, dict)\n"
-            "            else {}\n"
-            "        )\n",
-        ),
-    ]
-    for old, new in replacements:
-        count = text.count(old)
-        if count != 1:
-            raise RuntimeError(f"expected one recovery replacement, got {count}: {old!r}")
-        text = text.replace(old, new, 1)
-    recovery.write_text(text, encoding="utf-8")
-
-    vtext = version.read_text(encoding="utf-8")
-    version_replacements = [
-        ('DISTRIBUTION_VERSION = "16.2.4"', 'DISTRIBUTION_VERSION = "16.2.5"'),
-        ('PACKAGE_VERSION = "16.2.4"', 'PACKAGE_VERSION = "16.2.5"'),
-        (
-            'PACKAGE_RELEASE_NAME = "host-tool-provenance-memory-autoload"',
-            'PACKAGE_RELEASE_NAME = "static-type-audit-repair"',
-        ),
-    ]
-    for old, new in version_replacements:
-        count = vtext.count(old)
-        if count != 1:
-            raise RuntimeError(f"expected one version replacement, got {count}: {old!r}")
-        vtext = vtext.replace(old, new, 1)
-    version.write_text(vtext, encoding="utf-8")
-
-    for relative in (
-        ".github/workflows/v1625-type-audit-repair.yml",
-        ".github/.v1625-type-audit-repair-trigger",
-        "sitecustomize.py",
-    ):
-        path = root / relative
-        if path.exists():
-            path.unlink()
-
-    repository = os.environ.get("GITHUB_REPOSITORY", "SmuklyLew/jazn_latka")
-    _run(["git", "remote", "set-url", "origin", f"https://github.com/{repository}.git"])
-
-    child_env = dict(os.environ)
-    child_env[_GUARD] = "1"
-    _run(
-        [
-            sys.executable,
-            "-X",
-            "utf8",
-            "-m",
-            "latka_jazn.tools.release_metadata_sync",
-            "--root",
-            ".",
-            "--base-branch",
-            "master",
-            "--write",
-            "--json",
-        ],
-        env=child_env,
-    )
-
-    _run(["git", "diff", "--check"])
-    _run(["git", "config", "user.name", "github-actions[bot]"])
-    _run(["git", "config", "user.email", "41898282+github-actions[bot]@users.noreply.github.com"])
-    _run(["git", "add", "-A"])
-    staged = subprocess.run(["git", "diff", "--cached", "--quiet"], check=False).returncode
-    if staged == 0:
-        return
-    _run(["git", "commit", "-m", "fix: repair v16.2.5 static type audit"])
-    _run(["git", "push", "origin", f"HEAD:{_BRANCH}"])
-
-
-_repair()
